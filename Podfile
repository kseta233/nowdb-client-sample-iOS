# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'nownews' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for nownews
  pod 'Alamofire', '~> 3.4'
  pod 'SwiftyJSON', '~> 2.3'
  pod 'SDWebImage', '~> 3.8'
  pod 'SVProgressHUD'


  target 'nownewsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'nownewsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
